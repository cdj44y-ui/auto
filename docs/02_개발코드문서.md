# 근태관리시스템 (AES) 개발 코드 문서

**문서 버전:** 1.0  
**작성일:** 2026년 1월 13일  
**작성자:** Manus AI  
**프로젝트명:** ATTENDANCE ENTERPRISE SYSTEM (AES)

---

## 1. 프로젝트 구조

### 1.1 디렉토리 구조

```
attendance-system/
├── client/                          # 프론트엔드 코드
│   ├── index.html                   # HTML 진입점
│   ├── public/                      # 정적 파일
│   │   ├── images/                  # 이미지 에셋
│   │   ├── manifest.json            # PWA 매니페스트
│   │   └── pwa-*.png                # PWA 아이콘
│   └── src/
│       ├── App.tsx                  # 메인 앱 컴포넌트
│       ├── main.tsx                 # React 진입점
│       ├── index.css                # 전역 스타일
│       ├── _core/                   # 코어 훅
│       │   └── hooks/useAuth.ts     # 인증 훅
│       ├── components/              # 재사용 컴포넌트
│       │   ├── admin/               # 관리자 컴포넌트
│       │   ├── calendar/            # 캘린더 컴포넌트
│       │   ├── consultant/          # 자문사 컴포넌트
│       │   ├── employee/            # 직원 컴포넌트
│       │   ├── flexible-work/       # 유연근무 컴포넌트
│       │   ├── partner/             # 파트너 컴포넌트
│       │   ├── payroll/             # 급여 컴포넌트
│       │   ├── settings/            # 설정 컴포넌트
│       │   └── ui/                  # UI 기본 컴포넌트
│       ├── contexts/                # React Context
│       │   ├── AuthContext.tsx      # 인증 컨텍스트
│       │   ├── BrandingContext.tsx  # 브랜딩 컨텍스트
│       │   └── ThemeContext.tsx     # 테마 컨텍스트
│       ├── hooks/                   # 커스텀 훅
│       ├── lib/                     # 유틸리티 함수
│       ├── pages/                   # 페이지 컴포넌트
│       └── types/                   # TypeScript 타입
├── server/                          # 백엔드 코드
│   ├── _core/                       # 코어 모듈
│   │   ├── context.ts               # tRPC 컨텍스트
│   │   ├── cookies.ts               # 쿠키 관리
│   │   ├── env.ts                   # 환경변수
│   │   ├── oauth.ts                 # OAuth 처리
│   │   ├── trpc.ts                  # tRPC 설정
│   │   └── ...                      # 기타 코어 모듈
│   ├── db.ts                        # 데이터베이스 함수
│   ├── index.ts                     # 서버 진입점
│   ├── routers.ts                   # API 라우터
│   └── storage.ts                   # S3 스토리지
├── shared/                          # 공유 코드
│   ├── const.ts                     # 상수
│   └── types.ts                     # 공유 타입
├── drizzle/                         # DB 마이그레이션
│   ├── schema.ts                    # 스키마 정의
│   ├── relations.ts                 # 관계 정의
│   └── *.sql                        # SQL 마이그레이션
├── package.json                     # 의존성 정의
├── tsconfig.json                    # TypeScript 설정
├── vite.config.ts                   # Vite 설정
├── drizzle.config.ts                # Drizzle 설정
└── vitest.config.ts                 # Vitest 설정
```

### 1.2 주요 파일 설명

| 파일 | 역할 | 설명 |
|-----|-----|-----|
| `client/src/App.tsx` | 라우팅 | 모든 페이지 라우트 정의 |
| `client/src/main.tsx` | 진입점 | React 앱 마운트 |
| `server/routers.ts` | API | tRPC 라우터 및 프로시저 |
| `server/db.ts` | 데이터 | 데이터베이스 CRUD 함수 |
| `drizzle/schema.ts` | 스키마 | 테이블 정의 |

---

## 2. 프론트엔드 코드

### 2.1 페이지 컴포넌트

#### 2.1.1 페이지 목록

| 파일명 | 경로 | 접근 권한 | 설명 |
|-------|-----|----------|-----|
| `Home.tsx` | `/admin-dashboard` | admin | 관리자 대시보드 |
| `EmployeeDashboard.tsx` | `/employee-dashboard` | employee | 직원 대시보드 |
| `ConsultantDashboard.tsx` | `/consultant-dashboard` | consultant | 자문사 대시보드 |
| `DeveloperDashboard.tsx` | `/developer-dashboard` | developer | 개발자 대시보드 |
| `UnifiedLogin.tsx` | `/login` | 전체 | 통합 로그인 |
| `Employees.tsx` | `/employees` | admin | 직원 관리 |
| `Payroll.tsx` | `/payroll` | admin | 급여 관리 |
| `Attendance.tsx` | `/attendance` | admin | 근태 관리 |
| `Approvals.tsx` | `/approvals` | admin | 승인 관리 |
| `Contract.tsx` | `/contract` | admin | 전자 근로계약서 |
| `Workflow.tsx` | `/workflow` | admin | 전자 결재 |
| `Reports.tsx` | `/reports` | admin | 통계 보고서 |
| `Insights.tsx` | `/insights` | admin | AI 인사이트 |
| `Settings.tsx` | `/settings` | admin | 설정 |

#### 2.1.2 라우팅 구조 (App.tsx)

```tsx
// 주요 라우트 구조
<Switch>
  {/* 공통 진입점 */}
  <Route path="/login-gateway" component={LoginGateway} />
  <Route path="/login" component={UnifiedLogin} />
  <Route path="/employee-signup" component={EmployeeSignup} />
  
  {/* 루트 경로 → 로그인으로 리다이렉트 */}
  <Route path="/">
    <Redirect to="/login" />
  </Route>

  {/* 역할별 대시보드 */}
  <Route path="/employee-dashboard">
    <ProtectedRoute allowedRoles={["employee"]} component={EmployeeDashboard} />
  </Route>
  <Route path="/admin-dashboard">
    <ProtectedRoute allowedRoles={["admin"]} component={Home} />
  </Route>
  
  {/* 관리자 전용 페이지 */}
  <Route path="/employees">
    <ProtectedRoute allowedRoles={["admin"]} component={Employees} />
  </Route>
  {/* ... 기타 관리자 라우트 */}
</Switch>
```

### 2.2 주요 컴포넌트

#### 2.2.1 관리자 컴포넌트 (`components/admin/`)

| 컴포넌트 | 파일명 | 기능 |
|---------|-------|-----|
| 연차 관리 대장 | `AnnualLeaveLedger.tsx` | 연차 현황 조회/관리 |
| 근태 수정 | `AttendanceCorrection.tsx` | 근태 기록 수정 |
| 감사 로그 | `AuditLogViewer.tsx` | 보안 감사 로그 조회 |
| 브랜딩 설정 | `BrandingSettings.tsx` | 로고/색상 커스터마이징 |
| 직원 상세 폼 | `EmployeeDetailForm.tsx` | 직원 추가/수정 폼 |
| 직원 관리 | `EmployeeManagement.tsx` | 직원 목록 관리 |
| IP 접근 관리 | `IpAccessManager.tsx` | 허용 IP 관리 |
| 알림 설정 | `NotificationSettings.tsx` | 알림 규칙 설정 |
| 연장근무 리포트 | `OvertimeReport.tsx` | 연장근무 통계 |
| 급여 관리자 | `PayrollManager.tsx` | 급여 대장 관리 |
| 교대 스케줄러 | `ShiftScheduler.tsx` | 교대 근무 일정 |
| 가입 승인 | `SignupApproval.tsx` | 직원 가입 승인 |
| 근무시간 모니터 | `WorkHoursMonitor.tsx` | 주 52시간 모니터링 |
| 워크플로우 설정 | `WorkflowSettings.tsx` | 결재 워크플로우 |

#### 2.2.2 UI 컴포넌트 (`components/ui/`)

기본 UI 컴포넌트는 Radix UI를 기반으로 구현되어 있으며, Tailwind CSS로 스타일링됩니다.

| 컴포넌트 | 용도 |
|---------|-----|
| `button.tsx` | 버튼 |
| `dialog.tsx` | 모달 다이얼로그 |
| `select.tsx` | 드롭다운 선택 |
| `input.tsx` | 텍스트 입력 |
| `table.tsx` | 데이터 테이블 |
| `card.tsx` | 카드 컨테이너 |
| `tabs.tsx` | 탭 네비게이션 |
| `calendar.tsx` | 날짜 선택기 |
| `badge.tsx` | 상태 배지 |
| `avatar.tsx` | 사용자 아바타 |
| `MaskedData.tsx` | 민감정보 마스킹 |

### 2.3 Context API

#### 2.3.1 AuthContext

```tsx
// contexts/AuthContext.tsx
interface AuthContextType {
  user: User | null;
  isLoading: boolean;
  login: (role: string) => void;
  logout: () => void;
}

// 사용 예시
const { user, logout } = useAuth();
```

#### 2.3.2 ThemeContext

```tsx
// contexts/ThemeContext.tsx
interface ThemeContextType {
  theme: 'light' | 'dark';
  setTheme: (theme: 'light' | 'dark') => void;
}
```

#### 2.3.3 BrandingContext

```tsx
// contexts/BrandingContext.tsx
interface BrandingContextType {
  logo: string;
  primaryColor: string;
  companyName: string;
}
```

### 2.4 유틸리티 함수 (`lib/`)

| 파일 | 기능 |
|-----|-----|
| `utils.ts` | 일반 유틸리티 (cn 함수 등) |
| `trpc.ts` | tRPC 클라이언트 설정 |
| `annual-leave.ts` | 연차 계산 로직 |
| `audit-service.ts` | 감사 로그 서비스 |
| `calendar-utils.ts` | 캘린더 유틸리티 |
| `holidays.ts` | 공휴일 데이터 |
| `salaryCalculator.ts` | 급여 계산 |
| `taxEngine.ts` | 세금 계산 |
| `offline-queue.ts` | 오프라인 동기화 |

---

## 3. 백엔드 코드

### 3.1 API 라우터 (routers.ts)

#### 3.1.1 라우터 구조

```typescript
export const appRouter = router({
  system: systemRouter,        // 시스템 라우터
  auth: router({...}),         // 인증 라우터
  employee: router({...}),     // 직원 관리 라우터
  payroll: router({...}),      // 급여 관리 라우터
  admin: router({...}),        // 관리자 라우터
});
```

#### 3.1.2 RBAC 미들웨어

```typescript
// HR 권한 (admin, hr만 접근 가능)
const hrProcedure = protectedProcedure.use(async ({ ctx, next }) => {
  if (!ctx.user || !['admin', 'hr'].includes(ctx.user.role)) {
    throw new TRPCError({ 
      code: "FORBIDDEN", 
      message: "인사 관리 권한이 필요합니다." 
    });
  }
  return next({ ctx });
});

// Finance 권한 (admin, finance만 접근 가능)
const financeProcedure = protectedProcedure.use(async ({ ctx, next }) => {
  if (!ctx.user || !['admin', 'finance'].includes(ctx.user.role)) {
    throw new TRPCError({ 
      code: "FORBIDDEN", 
      message: "급여 관리 권한이 필요합니다." 
    });
  }
  return next({ ctx });
});
```

#### 3.1.3 API 엔드포인트 목록

| 라우터 | 프로시저 | 권한 | 설명 |
|-------|---------|-----|-----|
| `auth` | `me` | public | 현재 사용자 정보 |
| `auth` | `logout` | public | 로그아웃 |
| `employee` | `list` | hr | 직원 목록 조회 |
| `employee` | `getById` | hr | 직원 상세 조회 |
| `employee` | `create` | hr | 직원 등록 |
| `employee` | `bulkCreate` | hr | 직원 일괄 등록 |
| `employee` | `update` | hr | 직원 수정 |
| `employee` | `delete` | hr | 직원 삭제 |
| `payroll` | `getByPeriod` | finance | 기간별 급여 조회 |
| `payroll` | `getByEmployee` | finance | 직원별 급여 조회 |
| `payroll` | `create` | finance | 급여 기록 생성 |
| `payroll` | `confirm` | finance | 급여 확정 |
| `payroll` | `sendPayslip` | finance | 급여명세서 발송 |
| `payroll` | `bulkSendPayslips` | finance | 일괄 발송 |
| `admin` | `updateUserRole` | admin | 사용자 권한 변경 |

### 3.2 데이터베이스 함수 (db.ts)

#### 3.2.1 직원 관련 함수

```typescript
// 전체 직원 조회
export async function getAllEmployees(): Promise<Employee[]>

// ID로 직원 조회
export async function getEmployeeById(id: number): Promise<Employee | null>

// 직원 생성
export async function createEmployee(data: InsertEmployee): Promise<number>

// 직원 일괄 생성
export async function createEmployeesBulk(employees: InsertEmployee[]): Promise<number>

// 직원 수정
export async function updateEmployee(id: number, data: Partial<Employee>): Promise<void>

// 직원 삭제
export async function deleteEmployee(id: number): Promise<void>
```

#### 3.2.2 급여 관련 함수

```typescript
// 기간별 급여 조회
export async function getPayrollByPeriod(period: string): Promise<PayrollRecord[]>

// 직원별 급여 조회
export async function getPayrollByEmployee(employeeId: number): Promise<PayrollRecord[]>

// 급여 기록 생성
export async function createPayrollRecord(data: InsertPayrollRecord): Promise<number>

// 급여 기록 수정
export async function updatePayrollRecord(id: number, data: Partial<PayrollRecord>): Promise<void>

// 급여명세서 발송 표시
export async function markPayslipSent(id: number): Promise<void>
```

#### 3.2.3 이메일 로그 함수

```typescript
// 이메일 로그 생성
export async function createEmailLog(data: InsertEmailLog): Promise<number>

// 이메일 상태 업데이트
export async function updateEmailLogStatus(
  id: number, 
  status: 'sent' | 'failed', 
  errorMessage?: string
): Promise<void>
```

### 3.3 입력 스키마 (Zod)

```typescript
// 직원 생성 스키마
const createEmployeeSchema = z.object({
  employeeId: z.string().min(1),
  name: z.string().min(1),
  email: z.string().email().optional(),
  phone: z.string().optional(),
  department: z.string().min(1),
  position: z.string().optional(),
  joinDate: z.date().optional(),
  salary: z.number().optional(),
  bankAccount: z.string().optional(),
  bankName: z.string().optional(),
  status: z.enum(['active', 'leave', 'resigned']).optional(),
});

// 직원 수정 스키마
const updateEmployeeSchema = z.object({
  id: z.number(),
  name: z.string().min(1).optional(),
  email: z.string().email().optional(),
  // ... 나머지 필드
});

// 급여 생성 스키마
const createPayrollSchema = z.object({
  employeeId: z.number(),
  period: z.string().regex(/^\d{6}$/), // YYYYMM 형식
  baseSalary: z.number().min(0),
  overtimePay: z.number().min(0).optional(),
  bonus: z.number().min(0).optional(),
  deductions: z.number().min(0).optional(),
});
```

---

## 4. 데이터베이스 스키마

### 4.1 테이블 정의 (drizzle/schema.ts)

#### 4.1.1 users 테이블

```typescript
export const users = mysqlTable("users", {
  id: int("id").autoincrement().primaryKey(),
  openId: varchar("openId", { length: 64 }).notNull().unique(),
  name: text("name"),
  email: varchar("email", { length: 320 }),
  loginMethod: varchar("loginMethod", { length: 64 }),
  role: mysqlEnum("role", ["user", "admin", "hr", "finance"])
    .default("user").notNull(),
  department: varchar("department", { length: 64 }),
  createdAt: timestamp("createdAt").defaultNow().notNull(),
  updatedAt: timestamp("updatedAt").defaultNow().onUpdateNow().notNull(),
  lastSignedIn: timestamp("lastSignedIn").defaultNow().notNull(),
});
```

#### 4.1.2 employees 테이블

```typescript
export const employees = mysqlTable("employees", {
  id: int("id").autoincrement().primaryKey(),
  employeeId: varchar("employeeId", { length: 32 }).notNull().unique(),
  name: varchar("name", { length: 100 }).notNull(),
  email: varchar("email", { length: 320 }),
  phone: varchar("phone", { length: 20 }),
  department: varchar("department", { length: 64 }).notNull(),
  position: varchar("position", { length: 64 }),
  status: mysqlEnum("status", ["active", "leave", "resigned"])
    .default("active").notNull(),
  joinDate: timestamp("joinDate"),
  resignDate: timestamp("resignDate"),
  userId: int("userId"),
  salary: bigint("salary", { mode: "number" }),
  bankName: varchar("bankName", { length: 64 }),
  bankAccount: varchar("bankAccount", { length: 64 }),
  createdAt: timestamp("createdAt").defaultNow().notNull(),
  updatedAt: timestamp("updatedAt").defaultNow().onUpdateNow().notNull(),
});
```

#### 4.1.3 payroll_records 테이블

```typescript
export const payrollRecords = mysqlTable("payroll_records", {
  id: int("id").autoincrement().primaryKey(),
  employeeId: int("employeeId").notNull(),
  period: varchar("period", { length: 6 }).notNull(),
  baseSalary: bigint("baseSalary", { mode: "number" }).notNull(),
  overtimePay: bigint("overtimePay", { mode: "number" }).default(0),
  bonus: bigint("bonus", { mode: "number" }).default(0),
  deductions: bigint("deductions", { mode: "number" }).default(0),
  netPay: bigint("netPay", { mode: "number" }).notNull(),
  slipSent: boolean("slipSent").default(false),
  slipSentAt: timestamp("slipSentAt"),
  status: mysqlEnum("status", ["draft", "confirmed", "paid"])
    .default("draft").notNull(),
  createdAt: timestamp("createdAt").defaultNow().notNull(),
  updatedAt: timestamp("updatedAt").defaultNow().onUpdateNow().notNull(),
});
```

#### 4.1.4 email_logs 테이블

```typescript
export const emailLogs = mysqlTable("email_logs", {
  id: int("id").autoincrement().primaryKey(),
  recipientEmail: varchar("recipientEmail", { length: 320 }).notNull(),
  recipientName: varchar("recipientName", { length: 100 }),
  subject: varchar("subject", { length: 500 }).notNull(),
  emailType: mysqlEnum("emailType", ["payslip", "notification", "approval", "other"])
    .default("other").notNull(),
  referenceId: int("referenceId"),
  status: mysqlEnum("status", ["pending", "sent", "failed"])
    .default("pending").notNull(),
  errorMessage: text("errorMessage"),
  sentAt: timestamp("sentAt"),
  createdAt: timestamp("createdAt").defaultNow().notNull(),
});
```

### 4.2 마이그레이션 파일

| 파일 | 설명 |
|-----|-----|
| `0000_conscious_retro_girl.sql` | 초기 users 테이블 생성 |
| `0001_sparkling_gorgon.sql` | employees, payroll_records 테이블 추가 |
| `0002_dear_ravenous.sql` | email_logs 테이블 추가 |

---

## 5. 설정 파일

### 5.1 package.json 스크립트

```json
{
  "scripts": {
    "dev": "NODE_ENV=development tsx watch server/_core/index.ts",
    "build": "vite build && esbuild server/_core/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "start": "NODE_ENV=production node dist/index.js",
    "check": "tsc --noEmit",
    "format": "prettier --write .",
    "test": "vitest run",
    "db:push": "drizzle-kit generate && drizzle-kit migrate"
  }
}
```

### 5.2 환경변수

| 변수명 | 설명 | 필수 |
|-------|-----|-----|
| `JWT_SECRET` | JWT 서명 키 | ✅ |
| `OAUTH_SERVER_URL` | OAuth 서버 URL | ✅ |
| `OWNER_NAME` | 소유자 이름 | ✅ |
| `OWNER_OPEN_ID` | 소유자 OpenID | ✅ |
| `VITE_APP_TITLE` | 앱 제목 | ✅ |
| `VITE_APP_LOGO` | 앱 로고 URL | ❌ |
| `VITE_OAUTH_PORTAL_URL` | OAuth 포털 URL | ✅ |
| `BUILT_IN_FORGE_API_KEY` | Forge API 키 | ✅ |
| `BUILT_IN_FORGE_API_URL` | Forge API URL | ✅ |

### 5.3 TypeScript 설정 (tsconfig.json)

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "jsx": "react-jsx",
    "paths": {
      "@/*": ["./client/src/*"],
      "@shared/*": ["./shared/*"]
    }
  }
}
```

### 5.4 Vite 설정 (vite.config.ts)

```typescript
export default defineConfig({
  plugins: [react(), manusRuntime()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "client/src"),
      "@shared": path.resolve(__dirname, "shared"),
    },
  },
  server: {
    port: 3000,
    proxy: {
      "/api": "http://localhost:3001",
    },
  },
});
```

---

## 6. 주요 의존성

### 6.1 프로덕션 의존성

| 패키지 | 버전 | 용도 |
|-------|-----|-----|
| react | 19.2.1 | UI 프레임워크 |
| @tanstack/react-query | 5.90.2 | 서버 상태 관리 |
| @trpc/client | 11.6.0 | tRPC 클라이언트 |
| @trpc/server | 11.6.0 | tRPC 서버 |
| express | 4.21.2 | 웹 서버 |
| drizzle-orm | 0.44.5 | ORM |
| mysql2 | 3.15.0 | MySQL 드라이버 |
| zod | 4.1.12 | 스키마 검증 |
| lucide-react | 0.453.0 | 아이콘 |
| recharts | 2.15.2 | 차트 |
| xlsx | 0.18.5 | 엑셀 처리 |
| date-fns | 4.1.0 | 날짜 처리 |

### 6.2 개발 의존성

| 패키지 | 버전 | 용도 |
|-------|-----|-----|
| typescript | 5.9.3 | 타입스크립트 |
| vite | 7.1.7 | 빌드 도구 |
| tailwindcss | 4.1.14 | CSS 프레임워크 |
| vitest | 2.1.4 | 테스트 프레임워크 |
| drizzle-kit | 0.31.4 | DB 마이그레이션 |
| prettier | 3.6.2 | 코드 포맷터 |

---

*문서 끝*
